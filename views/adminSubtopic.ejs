<!DOCTYPE html>
<html lang="ru">
<head>
    <title>Админ-панель</title>
    <link rel='stylesheet' href='/stylesheets/navbar.css' />
    <link rel='stylesheet' href='/stylesheets/adminList.css' />
</head>
<header>
    <a href="/admin" class="admin_panel_main_page">Главная страница</a>
    <a href="/admin/add" class="add_anything">Новая тема</a>
    <a href="/admin/add-subtopic" class="add_anything">Новая подтема</a>
    <a href="/add-test" class="add_anything">Новый тест</a>
    <a href="/add-test-question" class="add_anything">Вопросы к тесту</a>
    <a href="/adminTopic" class="list_anything">Список тем</a>
    <a href="/adminSubtopic" class="list_anything">Список подтем</a>
    <a href="/admin/adminTest" class="list_anything">Список тестов</a>
</header>
<body>
<!--   <% if (messages.error) { %>
  <p style="color:red"><%- messages.error %></p>
<% } %> -->

<% if (messages.success) { %>
    <p class="alert alert-success mt-4"><%- messages.success %></p>
<% } %>
<div class="block_content">
    <h1>Список подтем</h1>
    <a href="/admin/add-subtopic">Создать новую подтему</a>
    <table class="table">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Номер подтемы</th>
            <th scope="col">Номер темы</th>
            <th scope="col">Название подтемы</th>
            <th scope="col">Описание подтемы</th>
            <th scope="col">Вопросы к подтеме</th>
            <th scope="col">Ответы к вопросам</th>
            <th>Действие</th>

        </tr>
        </thead>
        <tbody>
        <% if(data.length){
        for(var i = 0; i< data.length; i++) {%>
            <tr>
                <th scope="row"><%= data[i].id%></th>
                <td><%= data[i].idSubTopic%></td>
                <td><%= data[i].idTopic%></td>
                <td><%= data[i].nameSubTopic%></td>
                <td><%= data[i].descriptionTopis%></td>
                <td><%= data[i].questions%></td>
                <td><%= data[i].results%></td>
                <td>
                    <a class="edit" href="../admin/edit-subtopic/<%=data[i].idSubTopic%>">Редактировать</a>
                    <a class="delete" onclick="return alert('Are You sure?')" href="../admin/delete-subtopic/<%=data[i].idSubTopic%>">Удалить</a>
                </td>
            </tr>
        <% }
        }else{ %>
            <tr>
                <td colspan="3">No user</td>
            </tr>
        <% } %>
        </tbody>
    </table>
</div>
</body>
</html>